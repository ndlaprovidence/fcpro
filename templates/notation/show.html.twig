{% extends 'base.html.twig' %}

{% block title %}Notation{% endblock %}

{% block body %}
    <h1>Notation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notation.id }}</td>
            </tr>
            <tr>
                <th>Nom_formation</th>
                <td>{{ notation.nomFormation }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ notation.user }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ notation.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_notation_index') }}">back to list</a>

    <a href="{{ path('app_notation_edit', {'id': notation.id}) }}">edit</a>

    {{ include('notation/_delete_form.html.twig') }}
{% endblock %}
